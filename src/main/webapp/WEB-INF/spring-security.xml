<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <!-- enable use-expressions -->
   <!-- <http auto-config="false" use-expressions="true"> -->
    <http use-expressions="true">
        <headers>
            <cache-control />
        </headers>

        <intercept-url pattern="/admin" access="hasRole('admin')" />
        <logout logout-success-url="/" />
        <!--<access-denied-handler error-page="/403" /> -->

        <form-login login-page="/"
                    default-target-url="/admin/homepage"
                    authentication-failure-url="/login/error"
                    username-parameter="email"
                    password-parameter="password"
                    login-processing-url="/auth/login_check" />

        <logout logout-success-url="/j_spring_security_logout" delete-cookies="JSESSIONID" />
        <csrf />

    </http>

 <!--   <beans:bean id="authenticationManager"
                class="conference.model.AuthenticationManager" /> -->

    <!-- Select users and user_roles from database -->
    <authentication-manager>
        <authentication-provider>
        <user-service>
            <user name="test" password="12345" authorities="user" />
            <user name="admin" password="12345" authorities="admin" />
        </user-service>
        </authentication-provider>
    </authentication-manager>

    <!--    <authentication-manager>

            <authentication-provider>
                <password-encoder ref="encoder" />
                <jdbc-user-service data-source-ref="dataSource"
                                   users-by-username-query="select email, password, role from users where email=?"  />
            </authentication-provider>

        </authentication-manager> -->



    <!--
    <beans:bean id="encoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
        <beans:constructor-arg name="strength" value="10" />
    </beans:bean> -->

</beans:beans>